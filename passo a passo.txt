botar pip install -r .\requirements.txt




isso no main_bot.py
#mudar classe para mainbot
# em message factory usar uma f string f""


#import botbuilder.dialogs import Dialog
def __init__(self,
#             dialog: Dialog
#              conversation_state: ConversationState,
#              user_state: UserState
# ):
    #self.dialog = dialog
    #self.conversation_state = conversation_state
    #self.user_state = user_state

#async def on_turn(seld, turn_context):
    #await supper().on_turn(turn_context)

    #await self.conversation_state.save_changes(turn_context)
    #await self.user_state.save_changes(turn_context)